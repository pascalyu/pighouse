{% extends 'base.html.twig' %}

{% block title %}Pig{% endblock %}

{% block body %}
    <h1>Pig</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ pig.id }}</td>
            </tr>
            <tr>
                <th>PseudoName</th>
                <td>{{ pig.pseudoName }}</td>
            </tr>
            <tr>
                <th>Email</th>
                <td>{{ pig.email }}</td>
            </tr>
            <tr>
                <th>Password</th>
                <td>{{ pig.password }}</td>
            </tr>
            <tr>
                <th>CreatedAt</th>
                <td>{{ pig.createdAt ? pig.createdAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>LastUpdatedAt</th>
                <td>{{ pig.lastUpdatedAt ? pig.lastUpdatedAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>DeletedAt</th>
                <td>{{ pig.deletedAt ? pig.deletedAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>Username</th>
                <td>{{ pig.username }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('pig_index') }}">back to list</a>

    <a href="{{ path('pig_edit', {'id': pig.id}) }}">edit</a>

    {{ include('pig/_delete_form.html.twig') }}
{% endblock %}
