{% extends 'base.html.twig' %}

{% block title %}Edit Action{% endblock %}

{% block body %}
    <div class="offset-md-3 col-md-6 col-xs">
    <h1>Edit Action</h1>

    {{ include('action/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('action_index',{houseId:action.houseId}) }}">back to list</a>

    {{ include('action/_delete_form.html.twig') }}
    </div>
{% endblock %}
